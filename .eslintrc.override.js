// Configuración temporal para ESLint 9.x
// Si tienes problemas con linting, revertir a ESLint 8.x

module.exports = {
  root: true,
  extends: '@react-native',
  // Desactiva reglas problemáticas si es necesario
  rules: {
    // Agrega reglas personalizadas aquí si encuentras errores
  },
};
